% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{build_relabel_choices}
\alias{build_relabel_choices}
\title{Build Grouped Relabel Choices}
\usage{
build_relabel_choices(
  db_class_list = character(0),
  taxa_lookup = NULL,
  custom_classes = NULL
)
}
\arguments{
\item{db_class_list}{Character vector of class names from the global
class list (database).}

\item{taxa_lookup}{Data frame with at least a \code{clean_names} column.}

\item{custom_classes}{Data frame with at least a \code{clean_names} column.}
}
\value{
A named list with two elements: \code{grouped} (a named list
of character vectors for selectize optgroups) and \code{all} (a flat
character vector of all unique class names).
}
\description{
Merges the database class list, taxa lookup, and custom classes into
a grouped list suitable for \code{selectizeInput} with optgroups.
Database classes appear first, then taxa lookup classes not already
in the database, then custom classes.
}
\keyword{internal}
