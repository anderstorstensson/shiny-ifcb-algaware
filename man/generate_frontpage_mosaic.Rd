% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mosaics.R
\name{generate_frontpage_mosaic}
\alias{generate_frontpage_mosaic}
\title{Generate a numbered frontpage mosaic}
\usage{
generate_frontpage_mosaic(
  classifications,
  taxa_lookup,
  samples,
  raw_data_path,
  non_bio,
  n_images = 15L,
  wide_summary = NULL,
  scale_micron_factor = NULL,
  scale_bar_um = 5,
  temp_dir = tempdir()
)
}
\arguments{
\item{classifications}{Classification data.frame.}

\item{taxa_lookup}{Taxa lookup table.}

\item{samples}{Character vector of sample PIDs for this region.}

\item{raw_data_path}{Path to raw data directory (contains .roi files).}

\item{non_bio}{Character vector of non-biological class names to exclude.}

\item{n_images}{Number of taxa/images to include. Default 15.}

\item{wide_summary}{Optional wide-format summary from
\code{create_wide_summary()} for ranking taxa by biovolume concentration.}

\item{scale_micron_factor}{Optional numeric microns-per-pixel factor for
drawing scale bars in extracted PNGs.}

\item{scale_bar_um}{Scale bar length in microns. Default 5.}

\item{temp_dir}{Temporary directory for extracted PNGs.}
}
\value{
A list with \code{mosaic} (magick image) and \code{taxa}
(character vector of taxa in numbered order), or \code{NULL}.
}
\description{
Extracts one random image per top taxon, annotates each with a sequence
number, and composes them into a single mosaic.  Returns the mosaic image
together with the ordered taxa names for a figure caption.
}
