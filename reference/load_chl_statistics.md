# Load bundled historical Chl-a statistics (1991-2020)

Load bundled historical Chl-a statistics (1991-2020)

## Usage

``` r
load_chl_statistics()
```

## Value

A data.frame with columns: STATN, STNCODE, MONTH (integer), CHLA_mean
(numeric), CHLA_std (numeric), CHLA_n (integer).
