# CTD Module Server

CTD Module Server

## Usage

``` r
mod_ctd_server(id, config, rv)
```

## Arguments

- id:

  Module namespace ID.

- config:

  Reactive values with settings.

- rv:

  Reactive values for shared app state.

## Value

NULL (side effects only).
