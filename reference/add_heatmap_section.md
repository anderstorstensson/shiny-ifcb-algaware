# Add a heatmap section to the report (own page)

Add a heatmap section to the report (own page)

## Usage

``` r
add_heatmap_section(
  doc,
  wide_data,
  taxa_lookup,
  title,
  fig_num,
  cleanup,
  sample_counts = NULL
)
```
